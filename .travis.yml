language: go

sudo: false

go:
  - 1.7
  - 1.8

install:
  - go get github.com/alecthomas/gometalinter
  - gometalinter --install

script:
  - gometalinter --config=.gometalinter.json ./
  - go test -race -cover ./

notifications:
  email:
    on_success: never
    on_failure: always
